第一遍代码:
class Solution(object):
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """
        if x<0:
            return(False)
        l = 1
        while x>=10**l:
            l += 1
        for i in range(l/2):
            if ((x//(10**(l-1-i))) %10) != ((x//(10**i)) %10):
                return(False)
        return(True)
用时180ms。
